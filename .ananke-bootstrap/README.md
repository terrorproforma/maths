# Temporary ANANKE materialization payload

This checksum-pinned payload reconstructs the ANANKE v0 and v1 source snapshots, verifies both releases, commits the v0 milestone, advances the project to v1, regenerates derived artifacts, and then removes itself from the repository's final tree.
